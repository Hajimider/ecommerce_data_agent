# 电商数据分析 Agent 任务计划

## 目标

创建一个以 API 教师合成数据、本地 Qwen LoRA 知识蒸馏和三方评估为主线，并用 Agent 展示微调模型落地效果的项目。

## 阶段

- [x] 阶段 1：确定 API Agent、轻量 RAG、SQL 工具调用的设计。
- [x] 阶段 2：实现配置、知识检索、API 客户端和 MySQL 工具。
- [x] 阶段 3：实现 Agent 编排、命令行交互和安全重试。
- [x] 阶段 4：编写中文 README、样例配置并完成无 API Key 自检。
- [x] 阶段 5：实现教师模型合成数据、SQL 执行过滤和独立人工测试集。
- [x] 阶段 6：实现本地学生模型 LoRA 训练和数据检查。
- [x] 阶段 7：实现基础模型、LoRA、API 教师三方自动评估。
- [x] 阶段 8：增加 IDE 一键微调入口，验证轻量链路并重写 README。
- [x] 阶段 9：用批量 API 裁判替代逐条人工审核，增加置信度过滤、进度输出和配置说明。

## 下一步

API 裁判整改和轻量验证已完成；下一步用户可重新生成候选数据并运行训练。

## 错误记录

| 问题 | 处理 |
| --- | --- |
| 尚未提供 API Key | 提供 `.env.example` 和无 API Key 自检，不阻塞项目交付。 |
| PowerShell 默认编码读取中文计划时显示乱码 | 文件本身为 UTF-8；后续显式使用 UTF-8 读取，未改写为本机编码。 |
| PowerShell 单行测试中的 JSON 双引号被错误转义 | 改用 `json.dumps` 构造测试输入，重复 JSON 提取测试通过。 |
| 非法置信度单行测试再次被 PowerShell 引号截断 | 改用 `json.dumps` 构造输入后通过；项目自检本身未失败。 |
| 本机入口曾保存疑似真实 API Key 和模型绝对路径 | 已从入口移除，改为 `.env` 或本机未提交配置；旧 Key 需要用户在服务商后台撤销。 |
| 裁判配置可能部分回退到教师配置 | 改为三项成组配置，部分填写直接报错。 |

# 进度记录

- 已创建独立项目目录和中文设计说明。
- 已实现 API 客户端、轻量 RAG、SQL 安全校验、MySQL 工具和一次 SQL 修复重试。
- 已通过 `python app.py --self-test`；未配置 API Key，因此尚未进行真实 API 端到端调用。
- 已新增 `run_agent.py` 作为 IDE 一键启动入口，参数集中在文件开头；语法检查和自检均通过。
- 已为 HTTPS 自签名证书场景加入 Windows 证书库支持和可选 CA 证书配置；证书支持包已在 llm 环境中安装完成。
- 已新增 SQL 多行排版输出，并通过自检和三表连接样例验证。
- 已实现 API 与本地 Qwen 双模式切换；本地模型加载和最小生成已验证，控制台编码兼容已处理。
- 用户已确认将项目主线切换为“API 教师合成数据 → 本地 Qwen LoRA 蒸馏 → 三方评估 → Agent 落地演示”，阶段 5 开始实施。
- 已实现 15 条人工标准任务、API 中文改写、候选接受/拒绝记录、MySQL 执行过滤和 12 条独立人工测试题。
- `seed-only` 验证通过：15 条标准 SQL 全部可在教学 MySQL 库执行，训练集与候选记录已生成。
- 已实现 CPU LoRA 学生训练脚本，仅对 assistant JSON 回答计算损失；`--check-data` 对 15 条种子样本检查通过。
- 已实现基础模型、LoRA 学生和可选 API 教师的三方评估，统计提取率、可执行率、执行正确率、首次成功率、重试率、调用次数和耗时。
- 12 条独立测试集标准 SQL 均通过 MySQL 执行；一键入口导入、重复 JSON 提取和全项目语法编译通过。
- 交付前审查发现并修复 3 项问题：API 改写增加人工批准门槛；执行结果比较忽略列别名并按问题要求处理行序；训练拒绝覆盖非空适配器目录。
- 审查修复后的回归检查通过：应用审核、15 条训练数据检查、评估比较规则、Agent 自检和防覆盖负向测试均符合预期。
- 独立复核确认无剩余 Critical/Important 问题；README、设计说明和样例入口未发现本机绝对路径、用户名、解释器绑定或密钥。
- 用户要求取消逐条人工审核；已实现按种子批量调用 API 裁判，只有语义等价且置信度达到阈值的改写自动入训，裁判异常默认拒绝。
- 已增加独立裁判 API 配置、默认复用教师 API、逐种子进度输出和无网络裁判自检；公开示例默认不调用 API。
- 裁判自检、非法置信度拒绝、全项目编译、15 条种子临时链路、训练数据检查和可移植性扫描通过；未调用真实 API、未覆盖现有候选数据。
- 复核发现并修复安全问题：移除本机入口中的硬编码 API 密钥和模型路径；裁判地址、密钥、模型改为成组配置，半套配置直接拒绝。
- 复核发现并修复格式边界：布尔、重复或缺失字段的裁判 verdict 不再可能自动批准。
- 独立复核又发现 Agent 入口中的旧 Key、模型路径和数据库密码，已全部清除；两个 IDE 入口现在只保留空配置并从 `.env` 读取敏感信息。
- 最终回归通过：裁判自检、编译、Agent 自检、两个入口导入和入口敏感配置扫描均通过；未调用真实 API。

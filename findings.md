# 发现记录

- 已有 `text_to_sql` 项目提供 MySQL 教学数据库：`users`、`products`、`orders`、`order_items` 四张表。
- 新项目不修改旧项目，使用相同的数据库结构，但拥有独立配置和代码。
- Agent 运行可选 API 或本地模型；微调效果验证必须比较同一本地基础模型与“基础模型 + LoRA”，API 教师不作为微调前模型。
- 为降低错误标签风险，教师 API 只改写人工验证的问题，标准 SQL 由人工种子提供并经过 MySQL 执行校验。
- 独立测试题不发送给教师 API，也不进入训练集。
- MySQL 执行只能证明标准 SQL 可执行，不能证明 API 改写与 SQL 语义等价；规模化场景使用独立 API 裁判和高置信度阈值自动过滤，裁判异常或不确定时直接丢弃。
- 执行结果评估应忽略列别名；题目未明确要求排序时还应忽略行顺序，避免把语义等价 SQL 判错。
- LoRA 输出目录非空时必须停止并要求新目录，避免覆盖用户已训练的适配器。
- API 密钥、模型绝对路径和数据库密码不能写入 `run_agent.py` 或 `run_finetuning.py`；两个入口只保留空覆盖项，敏感值由 `.env` 提供。
